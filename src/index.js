/**
 * Name Of Person
 * @type {string}
*/

const name = 'youssef';

/**
 * lists
 * @type {Array<number>}
 */

 const listsOfNumbers = [1,8,48,47];

/**
  * @type {{id: number, text: string}}
*/

const object = {
      id: 1,
      text: 'hello docs'
}

/**
 * type a name
 * @param {string} name 
 * @return {string} name
 */

function printName (name){
    return 'My name is ' + ' ' + name;
};